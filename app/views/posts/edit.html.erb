<h1>Editing post</h1>

<% form_for([:admin, @post.show, @post]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :slug %><br />
    <%= f.text_field :slug %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit "Modifier" %>
    ou <%= link_to 'revenir', admin_show_post_path(@post.show, @post) %>
  </p>
<% end %>


